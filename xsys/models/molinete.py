# El grupo de molinetes (columna del monitor) se movió a
# ``institutions.DoorTurnstileGroup``. La puerta pasó a ser una entidad propia
# (``institutions.AccessDoor``) e independiente de xSys, así que este modelo ya
# no vive acá. Archivo vaciado a propósito (se conserva para no romper historial).
