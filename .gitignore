# Python / entorno
.venv/
venv/
__pycache__/
*.py[cod]
*.log
.DS_Store

# Django / base local / build de estáticos y media
db.sqlite3
db.sqlite3-journal
db.sqlite3-wal
db.sqlite3-shm
/staticfiles/
/media/

# Entorno: el .env real se ignora; el template .env.dist SÍ se versiona
.env
.env.*
!.env.dist

# Secretos / credenciales (NUNCA versionar)
**/vpn_credentials.txt

# Configs / estado runtime de VPN
access_control/proton_configs/
**/vpn_servidores_bloqueados.json

# Datos (socios / fallecidos) — no versionar
*.csv
*.xlsx

# Volcados grandes / backups / temporales
scripts/geba_xsys_2026.sql
/test_consulta.py
/test_resultado.txt
*_old.py

# HTML de debug
**/debug_get.html
**/debug_post.html

# IDE
.idea/
.vscode/

# Auto-deploy (scripts/auto_deploy.ps1)
auto_deploy.lock

# Descargas del navegador de la verificación ANSES: son artefactos de la corrida
# (88 MB de .crdownload a medio bajar), no código ni datos que haya que versionar.
descargas_anses/

# Scripts sueltos de diagnóstico en la raíz (pruebas puntuales contra molinetes,
# escuchas, etc.). Se usan y se tiran; no son parte de la aplicación.
_*.py
